<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.crc_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".util.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CRC_Android"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".view.viewmore.NoticecontentActivity"/>
        <activity android:name=".view.viewmore.NoticeActivity"/>
        <activity android:name=".view.viewmore.NoticeregistActivity"/>
        <activity android:name=".view.viewmore.AdminnoticeActivity"/>
        <activity android:name=".view.viewmore.ViewmoreActivity"/>
        <activity
            android:name=".view.login.LoginActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".view.register.RegisterActivity" />

    </application>

</manifest>